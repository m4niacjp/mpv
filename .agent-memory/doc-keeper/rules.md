# doc-keeper rules (mpv)

- Canonical docs directory is `DOCS/`, not `docs/`.
- User-visible player behavior: `DOCS/man/`. Interface notes: `DOCS/interface-changes/`.
- This checkout: `AGENTS.md`, `DOCS/local-workflow.md` for local-fork / Windows workflow.
- `doc-search` owns `DOCS/references/libraries/*.md`. Do not rewrite those bodies.
- Keep patches minimal; match existing tone. No Internal.md unless initialize is requested (project uses existing README/AGENTS/DOCS layout).
