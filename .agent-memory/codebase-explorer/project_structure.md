---
name: project structure
description: Top-level mpv repo folders
type: project
---

Skip: `build/`, `build-shared/`, `dist/`, `subprojects/`, `.venv/`, `node_modules/`
Key dirs: `player/`, `video/`, `options/`, `DOCS/`, `etc/`, `test/`
User visual quality lives primarily in Roaming `mpv.conf` + Lua, not upstream C.
