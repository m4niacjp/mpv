make `--autocreate-playlist` only activate for regular files

`--autocreate-playlist=filter|same` opens the requested local file immediately
and inserts sibling playlist entries in the background. Directory listing no
longer blocks the first frame. Opening a directory still lists it before
playback. The `playlist` property may grow after `file-loaded`.
